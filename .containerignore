.git/
.github/
*.test
*_test.go
testdata/
.env*
.mcp.json
.containerignore
.gitattributes
README.md
# .engine-src is the CI / local checkout of lexfrei/pogo-pvp-engine;
# the Containerfile receives that source via the `engine` named
# BuildKit context (COPY --from=engine), so sending it a second time
# through the default build context would bloat the builder layer.
.engine-src/
# LICENSE must reach the builder — BSD 3-Clause §2 requires the
# license text in every redistribution, including container images.
